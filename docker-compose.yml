services:
  backend:
    build: ./backend
    ports:
      - "127.0.0.1:3001:3001"
    env_file: .env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-}
    ports:
      - "127.0.0.1:3000:3000"
    env_file: .env
    restart: unless-stopped
    depends_on:
      - backend
