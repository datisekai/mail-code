# ── Gmail OAuth2 (see GMAIL_SETUP.md) ─────────────────────────────────────────
GMAIL_CLIENT_ID=your_client_id_here
GMAIL_CLIENT_SECRET=your_client_secret_here
GMAIL_REFRESH_TOKEN=your_refresh_token_here

# ── Access control ─────────────────────────────────────────────────────────────
# Share this URL with your team: http://YOUR_DOMAIN:3000/inbox/<ACCESS_TOKEN>
# Generate: openssl rand -hex 32
ACCESS_TOKEN=replace_with_32_char_random_string

# ── Frontend → Backend URL (browser-facing) ────────────────────────────────────
# Behind nginx (same domain): leave empty — frontend uses relative /api/ paths
# Local dev without nginx:    http://localhost:3001
NEXT_PUBLIC_API_URL=

# ── Optional ───────────────────────────────────────────────────────────────────
# CORS_ORIGIN=http://YOUR_VPS_IP:3000
PORT=3001
